# 更新日志

本文件记录本项目所有值得对外说明的变更。

- 格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循
  [语义化版本](https://semver.org/lang/zh-CN/)。
- **本文件只按版本组织，不按分支组织**：一节 = 一个 tag = 一次对外快照。
  逐条提交记录不在这里，看对应开发线分支的 `git log`（当前是哪条分支写在各节开头）。
- **只追加，不删旧节。** 要更正已发布版本的说法，往下新开一节写“更正”，不改原文。
- **本文件不构成任何许可授予**，也不得被用来放宽、替换或删减 README 末尾的权利声明。
  「不授予任何许可 / 全部权利保留 / 禁止商用（除非事先取得书面授权）」是既成条款，
  本文件只记录它的生效事实，不新增任何例外、免责或许可表述。
- 本文件所在的 `master` 分支**只放说明文件，不含实现代码**；实现与全量代码在开发线分支
  （形如 `dev` / `dev<日期>`，[分支清单](https://github.com/zhanghan1999/SupperHan/branches)，
  本节当前对应哪条写在该节开头）。因此下面的条目描述的是**能力**，不是本分支上的文件清单。

## [Unreleased]

下一快照尚未打 tag。本节记录 `v0.1.0` 之后落在
[`dev20260913`](https://github.com/zhanghan1999/SupperHan/tree/dev20260913) 的变更
（`83b805a` 数据库通道只读收口、`cf765d9` 资产标识符命名空间，两条均已推送到 `origin`）；
逐条实现细节看那条分支的提交记录，本节只记能力边界的变化。
`dev` 与 `dev20260912` 按既有决定仍停在 `v0.1.0`（= `16383c5`），不含上述两条提交。

> **一处如实声明**：本分支的 `README.md` 与 `docs/` 是**早期快照**，未随上述变更重写
> （实测本分支上仍有 55 处退役资产名与已过时的数量描述）。要看现行能力边界请以
> [`dev20260913`](https://github.com/zhanghan1999/SupperHan/tree/dev20260913) 上的同名文件为准；本台账
> 只按版本追加，正是为了在这种时候仍有一个可信的时间线。

### Security

- **数据库通道收口为无条件只读**：契约层不再提供“某个库可以写”的声明入口。判据从“识别这条语句
  有没有写副作用”（**无完备解**：以读形态出现的写、藏在函数体里的写都不含写关键词）换成“这条通道
  有没有写出口”（恒定、可静态审计、不随 SQL 语法演化）。旧的“禁写库名清单”整体退役 —— 实测它有三条
  静默放行路径：清单为空即全放开、运行期传空库名即全放开、清单装的是库名而运行期传的是 schema 名
  （两个命名空间的字符串永不相等，配置越正确、拦得越空）。
- **写数据的唯一合法产物是一份交人工执行的 SQL 工件**：六段齐备（目标标注 / 前置校验 / 变更语句包在
  `BEGIN;` 里且**故意不写** `COMMIT;` / 回滚与反向语句 / 证据链 / 禁用项声明），缺任一段按“未产出”处理，
  宁可回报缺口也不交半截文件。系统不再生成“复制粘贴即可跑”的写语句，也不代用户弹确认框；工件被明令
  禁止自动执行、进 CI、或作为任何驱动的入参 —— 一旦能被自动跑，它就从“建议”退回“命令”，本契约作废。
- **未知即拒**：空语句、只含注释的语句这类“证不出自己只读”的输入一律按写处理，不因“看起来无害”放行。
- 两个失败原因严格分开：**契约不授予写** 与 **连不上**。合并成一个“失败”会把人引去查网络，
  而问题从来不在网络。
- **代价如实记录**：自动化验证里“写库再回滚”这条路径永久降级为部分自动 —— 测试经应用自己的数据源
  连库，那条路不经过本层守卫；该边界改由用例形态（类级事务回滚）守，不由本层承诺。写门禁从
  “运行期可放行”变成“永远不可放行”：**改数据必须有人在场**，这是本条的设计目的，也是它唯一的用处。

### Changed

- **破坏性：子代理（subagent）与技能（skill）的标识符统一带命名空间前缀（namespace）**，与命令
  （command）通道同一规则（例：`bug-dev` → `supperH-bug-dev`、`data-fetch` → `supperH-data-fetch`）。
  旧名**不留别名**，一次性切断。
  动因是实测事故而不是整齐划一：同一台机器上两个启用的插件导出**逐字同名**的资产时，谁被加载由加载
  顺序决定且**不产生任何报错** —— 结果是安全红线写在没被加载的那份文件上，表现为“全部测试通过而
  运行期保护为零”。命令通道从未撞上，因为它们的名字从一开始就带前缀。
  **升级动作**：重装一次同步产物并**重启 IDE**（资产清单在启动时扫，改名前的会话手里还是旧名字）。
- **每个标识符配一个中文角色名**（开发 / 重构 / 测试编写 / 测试执行 / 取数协议 …），出现在资产自身的
  说明、正文标题与派单文案里。重命名只解决撞面，“看名字不知道它该派给谁”是同一个变更的另一半。
  角色名**不进入标识符本身**：非 ASCII 文件名要穿过构建 → 产物 → 插件缓存 → IDE 全局目录四段链路，
  任何一段处理不一致，失败方式仍然是“静默解析不到” —— 正是本条要治的病。
- 项目契约里 `db.writableUser`、`db.forbidWriteSchemas` 两个键与 `sql_write` 动作词一并退役。
  存量注册条目需人工删一次：校验器对它们**逐项点名报错并附迁移步骤**，不静默忽略，也不靠
  “未知字段”这种说不出该改什么的消息兑人。

### Added

- **构建期新增一道命名空间门禁**：缺前缀 / 技能声明名与目录名不符 / 资产目录被清空 / 退役旧名回潮
  —— 四种情形一律阻断构建（独立退出码，不与已有的纯度、行尾码混用），不再依赖人工审查。其中
  前两条判据**不依赖改名映射表**，所以“新增资产忘记登记”不会因为“表里没有这个名字”而被放行。
- **新增契约规范：SQL 工件六段式**，归入驱动契约；同时新增退役键清点判据（见上面 Changed 条）。
- 新增回归测试里包含**门禁自证**：在临时目录主动构造上述四种坏法，逐个断言门禁真会红 ——
  只测“当前状态是干净的”等于没测门禁。

## [0.1.0] - 2026-09-13

首个对外快照。定位：与具体项目无关的多 Agent 工作流工具层（L1），可复用到多个 Java 项目。

### Added

- **三层分离**：L1 通用行为层（本仓库）/ L2 项目契约层（同级私有根，装内网域名、库名、
  路径、凭据引用）/ L3 个人习惯层。真实项目事实不进公开仓库。
- **一键安装与双 IDE 适配**：一条命令完成建私有根骨架 → 探测 IDE（Qoder / OpenCode）→
  同步资产 → 安装；同步产物为可重复构建的快照目录。`--check` 形态做产物与源的一致性校验，
  不一致即报错，不降级、不静默通过。
- **项目注册**：扫目标工作区结构自动填事实，外部数据源改为**一次多选接入清单** ——
  不接 = 纯代码模式（明确报“配置了 0 个驱动”，不假装探测通过），接一半 = 写盘前拦截。
- **代码学习**：按 Controller 入口建调用链摘要，分批落盘 + 代次目录 + 原子切换 + 惰性回收；
  索引文件是机器契约（带 schema 代际号），代际不符一律拒绝，不做静默兼容。
- **修 bug 工作流**：快路径门禁 G0–G5 + 意图复述 I0。**全部判定下沉为脚本退出码**
  （0 直进 / 10–12 硬停 / 20–22 未注册 / 30–37 正常分流 / 40 停下来问用户），
  LLM 不做门禁裁决；新鲜度双级复核 —— 仓库级 + 批次级（学习数据的来源文件与目标提交的
  改动集求交集，不相交则放行，无从判定一律按不新鲜处理）。
- **驱动契约双通道**（脚本 / MCP 同构）：JSON 信封 + 退出码 0–5。外部源的**名字与个数归用户**，
  本仓库不持有任何数据源名单 —— 加一个源不改本仓库。探活必须是协议级判据（端口开放、
  网卡存在、ICMP 通都不算可达性证据）；写动作一律带确认 / 拒绝门禁；MCP 通道没有退出码，
  永不参与分流，只换取数。
- **发布防护**：占位符残留拦截 + 通用层纯度扫描（拿注册条目里的项目专有值比对全仓上传物，
  命中即拦，有例外通道与判定依据）。

### Changed

- 外部数据源从“必填”改为“可选且需显式声明”，模板不再携带可能被误用的假值。

### Deprecated

- 驱动槽位 `vpnPreCheck`（执行前预检）：实测无效 —— 零信任隧道网关对 VPN 网段的任意端口
  都代为 accept，端口探测 / 网卡名 / ICMP 在全断情况下均报绿灯。连通性只由各源的 `healthCheck`
  退出码**事后**判定。该键仍可被解析（否则已注册项目会一夜之间全部校验失败），
  校验只发废弃警告不阻断，运行期不再引用。

### Security

- 撤回误挂的 MIT 声明，改为**不授予任何许可、全部权利保留**；包元数据标记为不对外发布，
  对外快照去除个人账号信息。完整条款以 README 末尾「权利声明」为准，本节只是指向它。
- 生效边界：该声明适用于 2026-09-13 起的所有提交（自 `c79d530`）。此前版本出现的 MIT 字样
  系误挂，不构成授权；已下载旧版者在原 MIT 框架内的使用不受追溯撤销。
- 本节为既成事实的记录，**不构成新的授权、例外或豁免**；后续任何放宽都需权利人另行明确同意。

---

[unreleased]: https://github.com/zhanghan1999/SupperHan/tree/dev20260913
[0.1.0]: https://github.com/zhanghan1999/SupperHan/tree/v0.1.0

<!-- 不用 compare 链接（`/compare/v0.1.0...HEAD`）是刻意的：master 与 dev 两条历史无共同祖先，
     GitHub 的 compare 页对无公共提交的两条历史给不出差异。链接一律用 tag / 分支的 tree 直链。
     版本号的唯一可靠兜底是 tag：若某节对应的 tag 不存在，该版本号就是仓库里定位不到的字符串，
     宁可标“待补”，也不要提前写进本文件。 -->
