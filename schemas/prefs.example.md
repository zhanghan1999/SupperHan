# 个人习惯（L3 层）

> 本文件自由格式，agent 只参考、不作为分支判断依据。  
> 复制到 `../supper-Han-private/prefs.md` 后按个人口味改写；被 sync 忽略（不参与占位符替换）。  
> 若某段内容"必须被 agent 精确读取" → 移到 L2 `project.yaml`，不要留在本文件。

## 输出详略

- 修复摘要：简洁版 3-5 行；不列每个改动的文件（改动的文件在 diff 里看得见）
- 学习产物汇报：只报本次新增/合并的方法数，不复述 batch 内容
- 报错信息：完整贴 stderr；不省略 stack trace

## 交互习惯

- DB 门禁命中 → **不要弹二次确认**（红线优先），直接终止并汇报
- 派 subagent → 允许（不用先跟用户对齐每一次派发）
- 长时间任务 → 每 30s 输出一次心跳（"仍在跑 xxx"）

## 常用快捷

<!-- 例如："每次改 xxxController 之前提醒我先看 xxx 表结构" -->

## 踩过的坑（备忘）

<!-- 例如："上次改 fee 模块忘记同步 mapper.xml 导致 dev 环境炸" -->

## 其它

（自由书写区）
